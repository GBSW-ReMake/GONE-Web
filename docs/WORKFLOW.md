# 프론트엔드 개발 워크플로우

```text
Issue 확인
→ 계획과 완료 기준 확인
→ 기능 브랜치 생성
→ 구현
→ 정상·로딩·빈·오류·권한 상태 확인
→ pnpm lint/build
→ 커밋
→ dev 대상 PR
→ 검토 후 병합
```

작업 시작 전 Notion, Figma, 백엔드 API 문서를 확인한다.
API 계약이나 화면 정책이 바뀌면 관련 문서를 함께 수정한다.
